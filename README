Simple C and ncurses implementation of Conway's Game of Life.

RUNNING
Run 'clife' from bin directory to run life.
Universe is set to your terminal size.
First generation set randomly, by mouse or by keyboard.
